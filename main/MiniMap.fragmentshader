#version 330 core

in vec2 uv;
uniform sampler2D textureSampler;
out vec4 frag_colour;


void main () {

    frag_colour = vec4(1.0f);

	vec4 color = texture(textureSampler, uv);

	frag_colour = vec4(color.rrr, 1.0);

}